(function () { "use strict"; const byId=id=>document.getElementById(id); const esc=value=>String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c])); const qs=name=>new URL(location.href).searchParams.get(name)||""; if(!firebase.apps.length)firebase.initializeApp(window.firebaseConfig); const auth=firebase.auth(),db=firebase.firestore(); const orderId=qs("order"); function render(order={}){const paid=order.paymentStatus==="paid";byId("paymentReturnTitle").textContent=paid?"Payment confirmed":qs("cancelled")?"Payment cancelled":"Payment processing";byId("paymentReturnStatus").textContent=paid?"Your order is recorded and the next service step is underway.":qs("cancelled")?"Nothing was submitted. You can return and try again.":"Stripe confirmation can take a few seconds. This page updates automatically.";byId("paymentReturnDetails").innerHTML=`<div class="report-table"><div><span>Invoice</span><strong>${esc(order.invoiceNumber||"Pending")}</strong></div><div><span>Order</span><strong>${esc(orderId||"-")}</strong></div><div><span>Service</span><strong>${esc(order.orderType||"-")}</strong></div><div><span>Total</span><strong>$${(Number(order.amountCents||0)/100).toFixed(2)}</strong></div><div><span>Status</span><strong>${esc(order.paymentStatus||order.status||"pending")}</strong></div><div><span>Fulfillment</span><strong>${esc(order.fulfillmentStatus||order.shippingStatus||"pending")}</strong></div></div>`;} auth.onAuthStateChanged(user=>{if(!user){byId("paymentReturnTitle").textContent="Sign in to view payment";auth.signInWithPopup(new firebase.auth.GoogleAuthProvider()).catch(()=>{});return;}if(!orderId)return render({status:"missing order"});db.collection("serviceOrders").doc(orderId).onSnapshot(snap=>render(snap.exists?snap.data():{status:"not found"}),error=>{byId("paymentReturnStatus").textContent=error.message;});}); })();

